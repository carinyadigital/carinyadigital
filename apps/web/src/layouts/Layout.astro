---
import { Main } from '@carinya-digital/ui';
import Footer from '@/components/Footer.astro';
import Navbar from '@/components/Navbar.astro';
import { site } from '@/content/site';
import '@/styles/global.css';

interface Props {
  title?: string;
  description?: string;
  /**
   * Robots directive for this document. Error pages pass noindex so a
   * host that serves the not-found HTML with HTTP 200 still stays out of
   * search indexes.
   */
  robots?: string;
  /**
   * Canonical URL, or false to omit the tag. The not-found document is
   * reused for many missing paths, so a single canonical would point
   * crawlers at a URL that is not a real page.
   */
  canonical?: string | false;
}

const {
  title = site.name,
  description = site.description,
  robots,
  canonical,
} = Astro.props;

const canonicalHref =
  canonical === false
    ? undefined
    : (canonical ?? new URL(Astro.url.pathname, site.url).href);

const pageTitle = title === site.name ? title : `${title} · ${site.name}`;

const gtmId = import.meta.env.PUBLIC_GTM_ID;
const gtmEnabled = typeof gtmId === 'string' && /^GTM-[A-Z0-9]+$/i.test(gtmId);
---

<!doctype html>
<html lang="en-AU">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {gtmEnabled && (
      <script
        is:inline
        set:html={`(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','${gtmId}');`}
      />
    )}
    <meta name="description" content={description} />
    {robots ? <meta name="robots" content={robots} /> : null}
    <meta name="theme-color" content="#5C6A4A" />
    <link rel="icon" href="/favicons/favicon.ico" sizes="32x32" />
    <link rel="icon" href="/favicons/favicon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicons/favicon-16x16.png" type="image/png" sizes="16x16" />
    <link rel="icon" href="/favicons/favicon-32x32.png" type="image/png" sizes="32x32" />
    <link rel="apple-touch-icon" href="/favicons/apple-touch-icon.png" />
    <link rel="manifest" href="/favicons/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    {canonicalHref ? <link rel="canonical" href={canonicalHref} /> : null}
    <title>{pageTitle}</title>
  </head>
  <body class="flex min-h-dvh flex-col bg-background font-sans text-foreground">
    {gtmEnabled && (
      <noscript>
        <iframe
          src={`https://www.googletagmanager.com/ns.html?id=${gtmId}`}
          height="0"
          width="0"
          style="display:none;visibility:hidden"
        ></iframe>
      </noscript>
    )}
    <Navbar />
    <Main className="flex-1">
      <slot />
    </Main>
    <Footer />
  </body>
</html>
